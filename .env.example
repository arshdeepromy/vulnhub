# Copy to .env and fill in before `docker compose up`.
# Generate with: openssl rand -hex 16   (DB_PASS / DB_ROOT_PASS)
#                openssl rand -hex 32   (VH_ENC_KEY)
DB_PASS=
DB_ROOT_PASS=
VH_ENC_KEY=
