.env
db/
wp/wp-config.php
wp/wp-content/uploads/
wp/wp-content/debug.log
wp/wp-content/upgrade/
*.log
.admin_pass
.db_pass
.vh_enc_key
.mcp_token
.portal_test_pass

# Real/sensitive sample data — never commit.
# The EOS reconciliation drop is a live register: real hostnames, applications,
# risk ratings and decommission plans. It belongs on the box, not in a public
# repository, and no code reads it -- it was imported once through the screen.
dev/real-*.csv
wp/wp-content/plugins/vulnhub-eos/data/

# Browser-pass screenshots show whatever the stack holds -- on a real install,
# real hostnames, addresses and owners. Never commit them.
dev/shots/

# Local AWS access working notes — real account IDs, never commit.
docs/AWS-EXPOSURE-WIP.md

# Pick-up-here notes. Names real accounts and appliances by design; the
# sanitised version of everything in it is already in docs/. Never commit.
WORKLOG.md

# Session-start briefing: where prod/dev lives and how to work on it. Names
# real hosts and paths. Never commit.
SESSION-START.md
